# this will be used to generate a modules directory 
# we will need to list the modules too
# we will copy the modules from a directory and place them in a directory - 
# these modules are simply IDL code and C++ code that the IDL will bind with

